# factorial

# factorial seven is written 7!
# meaning 1 × 2 × 3 × 4 × 5 × 6 × 7

?Special

x <-  7

factorial(x)
gamma(x+1)

# natural log
lfactorial(x)
lgamma(x+1)

exp(lfactorial(x))
exp(1) ^ lfactorial(x)


plot(factorial(1:x), type = "l")
plot(lfactorial(1:x), type = "l")
