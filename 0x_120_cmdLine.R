# Running R from the command line	R is often run from an IDE, but that isn’t
# necessary. Being able to run code from a command line provides access to
# running code at scheduled times or adding it to a menu item.